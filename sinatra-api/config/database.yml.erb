default: &default
  host: <%= ENV.fetch('DATABASE_HOST', 'localhost') %>
  username: <%= ENV.fetch('DATABASE_USERNAME', 'app_user_development') %>
  password: <%= ENV.fetch('DATABASE_PASSWORD', '') %>
  database: <%= ENV.fetch('DATABASE_NAME', 'app_db_development') %>

development:
  <<: *default

production:
  <<: *default
